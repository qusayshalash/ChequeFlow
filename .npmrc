auto-install-peers=true
strict-peer-dependencies=false
# React Native / Expo's Metro bundler cannot follow pnpm's isolated symlink
# layout (native packages expect hoisted transitive dependencies), so the
# workspace uses a hoisted node_modules. See docs/architecture-decisions.md.
node-linker=hoisted
