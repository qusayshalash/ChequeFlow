# حالة المرحلة الثانية — إكمال دورة الشيك على الجوال

آخر تحديث: 2026-08-29

المرحلة الأولى بنت الخادم والويب. هذه المرحلة تُغلق الفجوة التي رصدها المستخدم:
معظم المنطق كان موجودًا في الـAPI بلا شاشة تستخدمه على الهاتف.

## ✅ ما تم إنجازه والتحقق منه فعليًا

| #   | البند                                               | حالة التحقق                                      |
| --- | --------------------------------------------------- | ------------------------------------------------ |
| 1   | إجماليات اللوحة منفصلة لكل عملة                     | مؤكَّد بنداء حي: ILS وUSD منفصلتان               |
| 2   | مجموعات «مسودة/محصّلة/معادة» — إصلاح أصفار اللوحة   | مؤكَّد: عملة كل شيكاتها مسودة صارت تظهر          |
| 3   | `isOverdue` محسوب على الخادم بتعريف واحد مشترك      | 6 اختبارات وحدة + نداء حي                        |
| 4   | فلترة `overdue` و`currency` في القائمة              | نداء حي (تحقق موجب وسالب)                        |
| 5   | المبلغ كتابةً (`amountInWords`)                     | ترحيلة + نداء حي                                 |
| 6   | سبب الإرجاع ومصاريفه (`bounceReason` / `bounceFee`) | ترحيلة + CHECK للسالب + نداء حي                  |
| 7   | كشف حساب جهة الاتصال لكل عملة                       | نداء حي                                          |
| 8   | حذف جهة الاتصال (أو تعطيلها إن كانت مرتبطة)         | نداء حي للحالتين                                 |
| 9   | دمج الجهات المكررة دون المساس بالسجل                | نداء حي + رفض الدمج بالنفس                       |
| 10  | إدارة المستخدمين والأدوار عبر API                   | نداء حي (إنشاء، تكرار، منع تعطيل الذات)          |
| 11  | تأجيل التنبيه وتأكيده وتنبيه يدوي لكل شيك           | نداء حي لكل الحالات                              |
| 12  | تصدير CSV مع تحصين ضد حقن الصيغ                     | 8 اختبارات وحدة + نداء حي (ترويسة + محتوى)       |
| 13  | تنسيق العملة `USD 4,000.00` بدل `$US`               | اختبار انحدار يرفض وجود `$`                      |
| 14  | التقويم الهجري خيار عرض                             | اختبار وحدة (1448 مقابل 2026)                    |
| 15  | شريط تنقل سفلي بخمس وجهات                           | حُزم التطبيق لـiOS وAndroid                      |
| 16  | سهم رجوع صحيح في RTL                                | مشتق من اللغة لا من `I18nManager`                |
| 17  | إضافة شيك يدويًا (نموذج كامل)                       | يستخدم مخطط الـAPI نفسه للتحقق                   |
| 18  | شاشة تفاصيل كاملة مع الصور                          | حُزمت                                            |
| 19  | تبويبات القائمة + ورقة تصفية وترتيب                 | حُزمت                                            |
| 20  | الاختبارات                                          | **239 وحدة + 45 تكامل + 49 نداء حي**، كلها ناجحة |

## كيف تم التحقق

الجهاز بلا Docker، فأُعيد نفس أسلوب المرحلة الأولى:

- **PostgreSQL حقيقي** عبر PGlite على البروتوكول السلكي — طُبِّقت الترحيلة الثالثة،
  ونُفِّذ الـseed، ومرّت 45 اختبار تكامل (منها 18 جديدة تغطي نقاط هذه المرحلة، فتلتقطها
  CI ولا تعتمد على تشغيل يدوي).
- **خادم S3 مصغّر** لمسار رفع الصور.
- **الـAPI المبنية شُغِّلت فعليًا** وجرى استدعاء كل نقطة نهاية جديدة عبر HTTP:
  49 فحصًا، كلها ناجحة.

## أخطاء حقيقية كشفها التشغيل الحي

1. **عملة تختفي من اللوحة كليًا.** قائمة العملات كانت تُشتق من المجموعات نفسها،
   فعملة كل شيكاتها مسودة لم تكن تظهر بأي رقم. صار الاشتقاق من كل شيكات المنشأة.
2. **الشيك المحصَّل لا يظهر في أي مؤشر.** لم تكن هناك مجموعة تغطي `CLEARED` —
   وهذا هو سبب بلاغ «اللوحة أصفار والقائمة مليئة». أُضيفت `cleared` و`draft`
   و`returned`.
3. **`toChequeSummary` كان عرضة لخطأ صامت.** بعد إضافة معامل `today`، كان
   `rows.map(toChequeSummary)` سيمرّر فهرس المصفوفة كتاريخ. جُعل المعامل مطلوبًا
   من نوع `string` فصار الاستدعاء الخاطئ خطأ ترجمة لا خطأ وقت تشغيل.

## غير مُنفَّذ عمدًا في هذه المرحلة

| البند                        | السبب                                                        |
| ---------------------------- | ------------------------------------------------------------ |
| استعادة نسخة احتياطية        | التصدير مُنفَّذ؛ الاستعادة تحتاج تصميم تعارضات لم يُطلب بعد  |
| تصدير PDF                    | CSV يغطي Excel وSheets؛ PDF يحتاج حزمة تخطيط عربي            |
| Push Notifications           | البنية جاهزة (حقل `channel`)، والتسليم يحتاج حساب FCM/APNs   |
| تعدد الشركات                 | المخطط يدعمه (`organization_id` في كل جدول)، ولا واجهة تبديل |
| تحديد عدة شيكات وإجراء جماعي | يحتاج تصميم تدقيق للإجراء الجماعي (من وافق على ماذا بالضبط)  |
| بصمة الدخول عند الإقلاع      | الزر يعمل ويتحقق؛ ربطه بحارس الإقلاع لم يُنفَّذ              |

## ما لا يزال غير قابل للتحقق على هذا الجهاز

- Docker Compose نفسه وMinIO الحقيقي (لا Docker).
- محاكي iOS (لا Xcode).
- نداءات Google Vision الحقيقية (لا مفاتيح — المزوّد يسقط إلى Mock بتحذير صريح).
